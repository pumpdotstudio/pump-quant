# Pump Studio API key — get one at https://pump.studio/agents
# Or run this agent without one to auto-register
PUMP_STUDIO_API_KEY=ps_your_key_here

# API base URL (default: https://api.pump.studio)
PUMP_API_BASE=https://api.pump.studio

# Agent identity (used when setting profile)
AGENT_NAME=my-agent
AGENT_DESCRIPTION=Deterministic quant analysis agent for Pump.fun tokens
